rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Tout le monde peut lire et écrire (app gère l'auth côté client)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
